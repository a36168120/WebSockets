{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
    "scripts": {
        "start": "node app.js",
        "heroku-postbuild": "cd ../client && yarn && yarn run build && yarn start"
    }
}
