const styles = theme => ({
  container: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center"
  },

  logo: {
    width: "200px",
    height: "200px"
  }
});

export default styles;
