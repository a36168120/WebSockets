const styles = theme => ({
  navBar: {
    height: "100px",
    display: "flex",
    justifyContent: "space-around",
    alignItems: "center"
  },

  navLinks: {
    textDecoration: "none",
    cursor: "pointer"
  }
});

export default styles;
